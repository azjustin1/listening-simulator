import { ChoiceMapPipe } from './choice-map.pipe';

describe('ChoiceMapPipe', () => {
  it('create an instance', () => {
    const pipe = new ChoiceMapPipe();
    expect(pipe).toBeTruthy();
  });
});
