import { IsCheckCellPipe } from './is-check-cell.pipe';

describe('IsCheckCellPipe', () => {
  it('create an instance', () => {
    const pipe = new IsCheckCellPipe();
    expect(pipe).toBeTruthy();
  });
});
